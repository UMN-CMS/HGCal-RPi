#!/bin/bash

source etc/config

./stop_pi_exes

# power cycle
for RDOUT_PI_ALIAS in "${RDOUT_PI_ALIASES[@]}"
do
    echo "power cycling $RDOUT_PI_ALIAS"
    # ssh $RDOUT_PI_ALIAS "cd $PI_RDOUTDIR; sudo ./bin/pwr_cycle_v1" &
    ssh $RDOUT_PI_ALIAS "cd $PI_RDOUTDIR; sudo ./bin/pwr_cycle_v2" &
done
wait
